<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".AddCourseActivity">

    <include
        android:id="@+id/include_add_course_toolbar"
        layout="@layout/toolbar" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/include_add_course_toolbar">
        <LinearLayout
            android:id="@+id/ll_add_course_detail"
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            >
            <include
                android:id="@+id/include_add_course_base"
                layout="@layout/item_add_course_base" />
            <include layout="@layout/item_add_course_detail" />
            <include layout="@layout/item_add_course_detail" />
            <include layout="@layout/item_add_course_detail" />
        </LinearLayout>
    </ScrollView>


</RelativeLayout>